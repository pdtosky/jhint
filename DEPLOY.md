# 인터넷 접속용 배포 안내

이 프로젝트는 이제 `Node.js` 서버로 실행되는 구조입니다.

## 로컬 실행

1. `run-server.bat` 실행
2. 브라우저에서 `http://localhost:3000` 접속

## 스마트폰/컴퓨터 연동 방식

- 예전처럼 `file://` 로 열면 기기 간 데이터 연동이 되지 않습니다.
- 반드시 서버 주소로 접속해야 합니다.
- 예: `http://localhost:3000` 또는 배포 후 `https://내도메인`

## 인터넷으로 접속하려면

아래 중 한 곳에 올리면 됩니다.

- Node.js 웹호스팅
- VPS
- 사내 서버
- 클라우드 앱 서버

## 배포 시 필요한 파일

- `server.js`
- `app.js`
- `index.html`
- `style.css`
- `data.json`
- `package.json`

## 시작 명령

```bash
npm start
```

## 중요한 점

- 현재 데이터는 `data.json` 파일에 저장됩니다.
- 따라서 배포 서버에서 이 파일이 유지되어야 데이터가 남습니다.
- 서버를 자주 초기화하는 무료 호스팅은 데이터가 사라질 수 있습니다.

## 추천 운영 방식

- 실제 회사용이면 `고정 저장공간이 있는 서버` 또는 `DB 연결 방식`으로 운영하는 것이 좋습니다.
- 다음 단계로는 `Supabase`, `Firebase`, `MySQL`, `PostgreSQL` 같은 온라인 DB 연결로 바꾸면 더 안정적입니다.
